import 'package:flutter/material.dart';

class NotificationPag extends StatelessWidget {
  const NotificationPag({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
